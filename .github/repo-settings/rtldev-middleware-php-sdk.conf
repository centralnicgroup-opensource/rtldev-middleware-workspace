# Overrides for rtldev-middleware-php-sdk.
#
# Applied on top of _baseline.conf and _profile-customer-facing.conf.

# Carried over from the classic branch protection this repository already had — it is
# the only repository in the organisation with real required checks configured, so these
# names are the surveyed truth rather than an inference.
REQUIRED_CHECKS="Lint: completed,Tests: completed"
