# Overrides for rtldev-middleware-devcontainer-features.
#
# Applied on top of _baseline.conf and _profile-internal.conf.
#
# The devbase Feature. Internal tooling for the department's devcontainers — issues stay
# off from the profile; its .github/ISSUE_TEMPLATE/ should be deleted in the same change
# that applies this, so the templates do not linger as files nothing can reach.

# Verified against the checks that actually report on this repository's default branch.
REQUIRED_CHECKS="Lint: completed"
